## 用户词典头部元数据
```
# Rime user dictionary
#@/db_name	luna_pinyin
#@/db_type	userdb
#@/rime_version	1.13.1
#@/tick	47
#@/user_id	PC-Windows
```

其中，tick值为上屏次数，从候选框选择一个汉字或词组视为一次上屏。

## 同步机制
rime 的同步机制为：点击同步后，导出临时用户词库，然后依次与同步文件夹下的词库进行合并。

> [!tips] 手动删除动态词库里的词条
> 选中该词，按 `Ctrl + Shift + Delete` 即可从db中删除，
> 再次同步时，db.txt 中该词条的 c 值会被取反，例如从 c=100 变为 c=-100
> 即 c 为负值的词条不在参与候选
> 可定期清理 db.txt 中 c 为负值的词条，保证动态词库精简，但这需要人为手动删除具体的词条才行
> 


## 试验
### 候选词顺序
假设：动态词库，同编码下，候选词顺序，~~感觉由d和c共同决定。~~

- 仅由d决定： 
    1 > 2 d1 > d2 c1 < c2 
    lu yi 	坐在	c=100 d=1.51057e-88 t=463
    lu yi 	左右	c=1 d=4.23232 t=463
- 结果:
    输入 lu yi 后，“左右”排在前面，及时“坐在”的c值远远大于对方。。。。

所以，仅在动态词库中的词，第一关键词为d，接下来应该是c。
但外部静态词库也会影响顺序，具体如何影响，有待研究。

但d值也远远大于对方啊，试验不严谨。。。

- [ ] 动态词库和静态词库如何共同影响候选词顺序？


## 自动拉取互联网热词到词库
希望可以自动从互联网上获取热词、新词，并加入词库。

- [ ] 如何获取热词？

- [ ] 如何保证词库的精简？


## 总结反思
输入法的核心痛点在于同编码下期望的候选词可能靠后。可否引入上下文机制，让上下文参与决定最终候选词的顺序？
方案一：听说小狼毫支持 Lua 脚本做候选词的后处理，可以部署本地小型排序模型，Lua 脚本拿到候选词和上下文后，让排序模型进一步处理，让最符合当前上下文的候选词排在前面。

还有便是自造词，这一点可以通过定期获取网络热词并加入词库来解决。

  


